# `class Arr`

```javascript
const arr=new Arr("array_name");

// set
arr.set([1,2,3]);

//push
arr.push(4);

//unshilft
arr.unshift(0);

//pop
arr.pop();

//splice
arr.splice(2,0);

//index
arr.index(1) // returns 0;

//edit
arr.edit(index, new-content);

// content
arr.content


```
